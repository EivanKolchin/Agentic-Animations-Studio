# One line ending, everywhere. The engine is run on Windows and Linux and
# a repo that rewrites every file on checkout has no readable diffs.
* text=auto eol=lf
*.png binary
*.webp binary
*.jpg binary
